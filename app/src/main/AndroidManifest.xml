<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.hibon.modepassesecurest">

    <uses-sdk android:minSdkVersion="15" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/mp_sr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mp_sr"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ihm.compte.ConnecteAccueilInterface"  android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ihm.compte.CreationCompteInterface"  android:windowSoftInputMode="stateHidden" />

        <activity android:name=".ihm.outipasses.Passes" android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ihm.outipasses.Analysor" android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ihm.outipasses.Generator" android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ihm.outipasses.Infos"  android:windowSoftInputMode="stateHidden" />

    </application>

</manifest>