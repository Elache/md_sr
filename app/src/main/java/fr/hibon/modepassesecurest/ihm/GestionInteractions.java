package fr.hibon.modepassesecurest.ihm;

import android.content.Context;
import android.support.v7.app.AlertDialog;
import android.view.LayoutInflater;
import android.view.View;

import fr.hibon.modepassesecurest.MainActivity;
import fr.hibon.modepassesecurest.R;

/**
 *
 */

public class GestionInteractions {

/*
    public static void interactGenerator(Context context, MainActivity activ) {
        LayoutInflater inflater = (LayoutInflater) context.getSystemService( Context.LAYOUT_INFLATER_SERVICE );
        View alertLayout = inflater.inflate(R.layout.generator, null);
        AlertDialog.Builder alert = new AlertDialog.Builder(activ);
        alert.setTitle("Génération paramétrée");
        // this is set the view from XML inside AlertDialog
        alert.setView(alertLayout);
        // disallow cancel of AlertDialog on click of back button and outside touch
        alert.setCancelable(false);
        AlertDialog dialog = alert.create();
        dialog.show();
    }
*/

}
